# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments
venv/
.venv*/

# Test & coverage artifacts
.coverage
.pytest_cache/
htmlcov/
pytest_report.xml

# Review-tool scratch output
.reports/

# Logs
*.log

# Architecture-review rollback backups
.audit-backup-*/

# Scratch outputs (generated CAD files, smoke artifacts)
output/*
# Governance docs live in output/ but must stay version-controlled (G1 backup spirit)
!output/optimization-plan-*.md
!output/architecture-evolution-*/
# Real-machine e2e acceptance ledger (better-harness F2): timestamped JSON reports stay ignored
!output/e2e-summary.md

# Root-level smoke/product artifacts (written to CWD by e2e runs and ring_light)
/part.STL
/part.step
/ring.generated.stl
/ring.layout.json

# Qoder workspace state (harness runs, local plans)
.qoder/

# SolidWorks lock files
~$*

# Local CodeGraph index
.codegraph/

# Independent nested project with its own repository
aicad/

# S5 T3 before-baseline worktree (aicad git worktree, contains .git link)
aicad-s5-before/
