# AI 驱动 3D 机械图全自动绘制——优化改进规划（第十期：能力完备波 G5/G6 + 评测 50 + G1 冲 95%）

> **生成**：2026-09-10 结构化会话（蓝本：`docs/product-vision.md` §4 差距矩阵 + §5「第八期（能力完备波）」顺延——见 vision §5 勘误注；用户指令「依 product-vision 立第十期」）
> **主题**：能力侧收官——GD&T 工具补全、评测集 30→50、CSG 双向互证、G1 冲 95%；G4 五族解锁待用户宏（U9）
> **用途**：供每日 02:30 自动实施任务读取执行；亦供人工实施参考（AGENTS.md 契约：自动任务读本文件=最新日期计划）
> **承接**：第九期 `output/optimization-plan-2026-09-10-2.md` N48-N51 **全部 ✅**（frozen 打包/D5 裁决/用户文档+24 卡示例库/G8 路由/G10 评估）。执行协议/安全红线/测试命令基线**继承第五期 §2 与第九期收官口径**（主仓 557+105；aicad 732 not-perf+18 deselected）。文件名 `-3` 后缀=同日第三份计划（排序仍为最新）。

---

## 1. 事实基线（2026-09-10 立项取证）

| 事实 | 状态 | 对本期任务的意义 |
|---|---|---|
| G1 交付合格率 | **90%**（N47 终验 27/30 单次连跑，first_pass 25/30）；残留 3 败全 assert_mismatch：two_step_bore_plate 体积超窗上界 2.1%、b7_flange_bolts/b7_frame_stack 装配解释差异 | N56 的收敛工作清单；距 95%≈1.5 任务（30 口径）|
| G2 评测集 | 30 任务（N45 扩容，离线 30/30） | N53 第二波 → 50 |
| G5 GD&T | 形位公差框格「尚未作为稳定工具暴露」（审查 G5 遗留）；drawing 域已有 tolerance/surface_finish 前例 | N52 取证→TDD→实现 |
| G6 CSG 契约 | v2 仅重建方向（polygon_prism/swept_arc 两首建 op）；通道 B AST 识别 polygon 延后 | N54 补导出方向+识别扩展 |
| G4 五族 | mirror/pattern/rib/combine/AutoBalloon BLOCKED（数学替代+如实声明）；宏录制器=唯一已知解锁路径（D4 裁决：用户每族 ~30min 录宏） | N55 条件任务，等 U9 |
| G7 装配 10+ 件 | ✅ 实质收口（b7_pump_base 10 件 PASS、plan 4/5 兑现——vision 已回填） | 残留并入 G1 |
| 产品化基础 | frozen 分发+用户文档+示例库+路由指南全落地（第九期） | 本期专注能力侧，产品面冻结 |

## 2. 任务进度总览

状态：`[ ]` 待执行 · `[~]` 进行中 · `[x]` 完成 · `[!]` BLOCKED。「仓」列：主=主仓库，ai=aicad。

| ID | 优先级 | 标题 | 仓 | 依赖 | 预估 | 状态 | 完成日期 |
|---|---|---|---|---|---|---|---|
| N52 | P1 | G5 GD&T：形位公差框格+基准符号工具（取证→TDD→实现，drawing 域） | 主 | 无 | 1晚 | `[x]` 2026-09-10（契约=IDrawingDoc.NewGtol 零参工厂+SetFrameSymbols2 九参/SetFrameValues2 六参/SetPosition+GetFrameCount 判据；GCS/MC 枚举 PS 反射；**工具 79→80 计数锁 5 处同步+README/AGENTS**；+8 fakes 测（含 NewGtol=None 诚实失败）；全套 565+106 绿；e2e 留观察项=SW 未运行窗口，实机 NewGtol 行为待定谳） | 2026-09-10 |
| N53 | P0 | G2 第二波：评测集 30→50（编辑型/多实体/参数化/装配变体/异形件域；断言全机械可验） | ai | 无 | 1-2晚 | `[x]` 2026-09-10（**离线 50/50 全绿**旧 30 零回归；6 个 mock/断言错被评测网首跑抓住；test_evals 同步+全套 732 绿；live 抽验 **1/5 如实记录**——4 败全 assert_mismatch（t_stiffener 措辞歧义观察项/csg .cut() 风格不识别=N54 输入/2 解释差异）；执行偏差：图纸域 3 名额改配（无断言键）） | 2026-09-10 |
| N54 | P2 | G6 CSG 契约 v3：通道 B AST 识别 polygon/extrude + 导出方向 op（双向互证对称化） | 主+ai | 无 | 1晚 | `[ ]` | |
| N55 | P1 | G4 宏录制器契约转录：五族逐族 .swp 宏→API 契约→BLOCKED 解锁（mirror/pattern/rib/combine/AutoBalloon） | 主 | **U9（用户录宏）** | 1晚/族 | `[ ]` 条件 | |
| N56 | P0 | G1 冲 95% 终验：50 任务全量连跑 + 残留败收敛裁决（反目标：不为凑分改断言） | ai | N53（新尺子）；N52 并行不阻塞 | 1晚 | `[ ]` | |

> 执行顺序建议：N52 与 N53 互相独立可并行开工（N52 走主仓取证+e2e，N53 走 aicad 离线评测——两者验证通道不同：GD&T 是 SW drawing 域工具，不走 build123d 沙箱，评测集无需等 N52）→ N56 收官；N54 穿插；N55 等宏逐族做。
> 远期观察项（继续继承）：RAG embedding 升级、通用 mate CSP、G9 尾项（安装器 UI/许可激活——不见真实用户不立项）、S5 观察项（GLM 通道补 T5、规划轮路由非 flash 档）。

### 1.4 用户动作清单（顺承第九期）

| ID | 动作 | 状态 |
|---|---|---|
| **U4（顺承五期）** | `coderabbit auth login`（本人浏览器 OAuth 或 API key） | ⏳ |
| **U8（顺承七八九期）** | GitHub Billing 失败发票清算（五轮 re-run 实证仍拦；Spending limit=50 已设，剩余拦因=失败发票；billing 页无欠款仍拦则提工单） | 🔴 |
| **U9（顺承九期，G4 解锁=本期 N55 前置）** | 宏录制器对照：SW 里录制 mirror（或任一 BLOCKED 族）宏 ~30min，产出 .swp 后一句话通知——N55 即刻开工该族 | ⏳ 条件 |

---

## 3. N52 G5 GD&T 工具（P1，主仓，1晚）

**目标**：制造级图纸的最后一块——形位公差框格（ InsertGTOL 家族）+ 基准符号作为稳定工具暴露。

- [x] 1. **取证探针**（探针先行，ADR-0011 路径）：typelib grep GTOL/Datum 符号族（InsertGTOL/InsertDatumTag/GetGTOL...），实测调用形态（参照 drawing 域既有坑：InsertModelAnnotations3 宿主错误前例、零参属性语义）。
- [x] 2. **TDD**：fakes 层工具契约测试（框格创建/参数/失败形态）；工具计数锁 79→80+（5 处断言同步 + README + AGENTS 基线，N36 纪律同 commit）。
- [x] 3. **实现**：drawing 域注册 `drawing_insert_gtol`（+基准符号视取证结果）；单位/语义对齐既有 tolerance 工具。
- [!] 4. **e2e**（SW 窗口）：SW 未运行窗口——按预置条款记观察项（实机 NewGtol 行为/框格落图判据待 SW 窗口定谳；fakes 全绿+契约取证为收口基础）：建件→出图→插框格→导出 PDF，PDF 含 GTOL 框格字节证据（或如实记录不可验）。
- [x] 5. 回填本表；全套绿；**BLOCKED 风险如实声明**（GTOL 族若全变体静默 None=AutoBalloon 同族，转观察+数学替代评估）。

**验收**：工具经 MCP 可调且返回结构化结果；离线 fakes 测试绿；e2e 一件过或如实 BLOCKED 定谳。
**反目标**：不硬凑（静默 None 也标注成功）；不一次做全 GB 框格全集（首版=常用三五种）。

## 4. N53 G2 第二波：评测集 30→50（P0，aicad，1-2晚）

**目标**：同一把尺子加宽到 50——覆盖 product-vision G2 标注的剩余域。

- [x] 1. **域分配**（+20 任务靶向）：编辑型深化（edit_* 3：改阵列/改材料后复检/特征抑制恢复）、多实体与装配变体（4：阶梯套筒组/法兰对/底架 12 件/变径管组）、参数化与方程式（3）、异形件（polygon/extrude 组合 3：指形件/T 型加强/斜切座）、材料/质量断言（2）、CSG 变体（2：对 v3 对齐）、图纸导出域（aicad 沙箱可断言的 3：BOM 行数/视图存在性/导出字节头）。**断言全部机械可验（离线 mock 可跑）**。
- [x] 2. **离线 50/50 全绿**；旧 30 零改零回归；**test_evals 任务数断言 30→50 同步**（N45 教训：改 tasks.jsonl 必须跑 pytest 全套）。
- [x] 3. live 抽验 5 个新任务 ≥3 过（local-relay 通道；新任务首跑失败如实记录——扩容目的就是暴露）。**实际 1/5**（cone PASS；败件定性：t_stiffener=prompt 措辞歧义（模型自洽读法 y=58 伸出接法）→与 mirror_bracket 同队列观察项；csg_counterbore_block=AST 不认 .cut() 方法风格→N54 扩识别面数据输入；base_frame_12/edit_fillet=解释差异）。低于验收线如实记录。
- [x] 4. 回填本表 + AGENTS 基线。

**验收**：离线 50/50；旧 30 零回归；pytest 全套绿；live 抽验留痕（过/不过都记录）。
**反目标**：不造简单靶任务凑数（product-vision §6 反指标）；不为 live 通过率改断言。

## 5. N54 G6 CSG 契约 v3：导出方向（P2，主仓+ai，1晚）

**目标**：双引擎互证对称化——v2 只有「SW 特征序列→build123d 重建」，v3 补「build123d 脚本→SW op 序列」方向。

- [ ] 1. **通道 B AST 识别扩展**（aicad 沙箱侧）：现有识别面盘点→补 `RegularPolygon`/`extrude`（v2 延后项）；识别失败的 op 如实归类（不猜）。
- [ ] 2. **导出方向 op 扩**（主仓 `rebuild_csg` 通道）：与新识别面一一对齐；roundtrip 互证（polygon/extrude 双向逐位对比，参照 v2 的 ring 2.9e-16 标准）。
- [ ] 3. **互证矩阵报告**：双向覆盖表（哪些 op 双向、哪些单向、哪些不可达）——诚实边界表。
- [ ] 4. 回填本表；双仓全套绿。

**验收**：至少 polygon_prism 一族双向逐位一致；不可达项有归因不硬凑。

## 6. N55 G4 宏录制器契约转录（P1，主仓，BLOCKED on U9，1晚/族）

**目标**：BLOCKED 五族的唯一已知解锁路径——用户宏（ground truth）→ API 契约转录 → 工具实现。

- [ ] 1. **宏解析器**（一次性基建）：.swp（SW VBA 宏）→ API 调用序列提取（swAPI 调用名+参数序）→ 与 typelib 签名对照输出「契约卡」。
- [ ] 2. **逐族转录**（每族一晚）：用户每交一族宏 → 契约卡 → 探针复现实测 → TDD → 工具实现/计数锁同步 → **BLOCKED 状态翻绿（docstring+tools/INDEX+示例卡同步）**。
- [ ] 3. 优先族建议：mirror（阵列改型场景最高频）→ pattern → rib → combine → AutoBalloon。
- [ ] 4. 回填本表（逐族记录）。

**验收**：每族=工具可用+计数锁同步+原 BLOCKED 声明撤销+示例库补卡。
**反目标**：无宏不猜（探针矩阵已证 13 变体直调全零产出——不再盲试）。

## 7. N56 G1 冲 95% 终验（P0，aicad，1晚，依赖 N53）

**目标**：北极星冲刺——在新尺子（50 任务）上取全量数字 + 残留败收敛。

- [ ] 1. **50 任务全量 live 连跑**（空闲机+local-relay+16k；baseline=N47 终验 27/30——口径注明 30→50 不可直比，分列「旧 30 子集」对照）。
- [ ] 2. **残留败收敛裁决**：N47 三败的处置数据复跑（two_step_bore_plate 窗口边际——裁决窗口是否过紧【改断言需用户批准】；b7 两件解释差异——观察 plan 路由非 flash 档机会）+ 新任务新败逐件定性。
- [ ] 3. **G1 终验报告**：50 口径合格率+first_pass+归因分层；vision §4/§6 回填。
- [ ] 4. 回填本表。

**验收**：全量报告落盘；95% 达成或有数据支撑的差距分解；**反目标：不为凑 95% 改断言/措辞（product-vision §6 反指标+产品原则 3）**。

---

## 8. 风险与未决（诚实披露）

- **N52 GTOL 族可能是 AutoBalloon 同族 BLOCKED**（静默 None）——取证首晚即定谳，BLOCKED 则如实声明+观察项，不阻塞本期其他任务。
- **N55 整体 BLOCKED on U9**（用户录宏）——无宏不猜（探针矩阵已证盲试无效）；本期无 U9 时 N55 留条件位。
- **N53 新任务 live 首跑可能拉低数字**——分列「旧 30/新 20」口径（N45 先例）；N56 的 95% 在 50 口径上评估，与 30 口径不可直比（诚实声明）。
- **U8 未解**：本期推送的 CI 验证继续挂起（同挡板）；本地全套绿是收口基础。
- 主仓工具计数将因 N52/N55 变化（79→80+）——5 处断言+README+AGENTS 基线同 commit 纪律全期维持。
