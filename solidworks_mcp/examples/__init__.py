"""Product-specific example tools kept outside the generic API layer.

Modules here implement one-off designs (e.g. the MV-LRSS-H-80-W ring
light) rather than reusable SolidWorks operations. The MCP server still
registers them as tools, but new generic capabilities belong in
``solidworks_mcp.solidworks_api`` instead of this package.
"""
